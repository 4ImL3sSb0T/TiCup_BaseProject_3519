[project]
name = "baseproject-scripts"
version = "0.1.0"
description = "Utility scripts for BaseProject_3519 (map analysis, motor serial tools, host UI)"
requires-python = ">=3.14"
dependencies = [
    "pyserial>=3.5",
    "customtkinter>=5.2",
    "matplotlib>=3.8",
]

[tool.uv]
package = false
