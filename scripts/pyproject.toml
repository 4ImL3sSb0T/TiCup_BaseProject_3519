[project]
name = "baseproject-scripts"
version = "0.1.0"
description = "Utility scripts for BaseProject_3519 (map analysis, motor serial tools)"
requires-python = ">=3.12"
dependencies = [
    "pyserial>=3.5",
]

[tool.uv]
package = false
